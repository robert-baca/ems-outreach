// Returns the hospital ID for the current request.
// TODO: replace body with auth session lookup when auth is added.
// e.g. return getSessionUser(req).hospitalId
export function getHospitalId(req) {
  return 'grapevine';
}
